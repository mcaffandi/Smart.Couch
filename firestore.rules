rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Izinkan akses jika user sudah login ke Firebase (Google Auth)
      allow read, write: if request.auth != null;
    }
  }
}
